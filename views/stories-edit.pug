extends layout.pug

include utils.pug

block content
    form(action=`/stories/${story.id}/edit` method='post' class='stories-create__container')
        input(type='hidden' name='_csrf' value=csrfToken)
        input(type='hidden' name='userId' value=userId)
        p(class='stories-create__title')= story.title
        +fieldNoLabel('stories-create__subtitle', 'subtitle', story.subtitle, 'text', 'Subtitle...')
        +fieldNoLabel('stories-create__image', 'pictureURL', story.pictureURL, 'url', "Image Link...")
        +fieldNoLabel('stories-create__body', 'body', story.body, 'textarea', 'Write your story here...')
        div(class='stories-create__button-container')
            button(class='stories-create__button-post' type="submit")
                img(title='Submit' src='/icons8-checked-26.png' class='submit__icon icon' alt='Submit')
            a(href=`/stories/${story.id}`)
                img(title='Cancel' src='/icons8-cancel-26.png' class='delete__icon icon')
        +validationErrorSummary(errors)
    link(rel='stylesheet' href='/stylesheets/stories-create.css')
